It was left as an exersize to show that given
$$H = \int d^3 p \left( { \frac{1}{{2}} \pi^2 + \frac{1}{{2}} \left( { \boldsymbol{\nabla} \phi} \right)^2 + \frac{m^2}{2} \phi^2 } \right),$$
we obtain
$$H_{\text{after}} =\int d^3 x \omega_\mathbf{p}\left( { a_\mathbf{p}^\dagger - i \frac{\tilde{j}^{*}(p)}{\sqrt{2 \omega_\mathbf{p}}} } \right)\left( { a_\mathbf{p} + i \frac{\tilde{j}(p)}{\sqrt{2 \omega_\mathbf{p}}} } \right),$$
If I do this problem, I get something close to this
$$H_{\text{after}} =\frac{1}{{2}}\int d^3 x \omega_\mathbf{p}\left( {\left( { a_\mathbf{p}^\dagger - i \frac{\tilde{j}^{*}(p)}{\sqrt{2 \omega_\mathbf{p}}} } \right)\left( { a_\mathbf{p} + i \frac{\tilde{j}(p)}{\sqrt{2 \omega_\mathbf{p}}} } \right)+\left( { a_\mathbf{p} + i \frac{\tilde{j}(p)}{\sqrt{2 \omega_\mathbf{p}}} } \right)\left( { a_\mathbf{p}^\dagger - i \frac{\tilde{j}^{*}(p)}{\sqrt{2 \omega_\mathbf{p}}} } \right)} \right)$$
The operator products in the integrand has a structure like $$\hat{Z}^\dagger \hat{Z} + \hat{Z} \hat{Z}^\dagger$$, which is almost like a $$2 \times \text{Real}$$ operation.  Assuming my calculation above is correct, what reason do we have to drop the second pair of operator products in the integrand?
